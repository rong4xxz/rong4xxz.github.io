---
title: MoE 架构
date: 2025-07-15
category: LLM
---

# 多专家混合架构（Mixture of Experts, MoE）

这是一种通过动态激活部分模型参数来提升模型效率的神经网络设计方法，其核心思想是**将任务分解，由多个“专家”子网络（即小型神经网络）协同处理**，同时通过门控机制（Gating Network）动态选择最相关的专家组合。

## 核心组件

- **专家（Experts）：**多个独立的子网络（如前馈神经网络），每个专家擅长处理特定类型的输入
- **门控机制（Gating Network）：**根据输入数据生成权重分布，决定哪些专家被激活（如 Softmax 输出权重，仅保留 Top-K 专家）

其工作原理：对于每个输入，仅激活少数专家，其余保持闲置。例如一个 1000 专家的 MoE 模型可能每次只调用 2 个专家，显著减少计算量

## 优势

- 高效扩展性：模型总参数量可以极大，但实际计算消耗仅与激活的专家相关
- 任务专业化：不同专家可专注于输入数据的不同特征（如语言、视觉或多模态任务）
- 灵活部署：适合分布式计算，专家可分布在不同设备上

# 代码实现

在 MiniMindModel 中通过替换 Transformer 块中标准的前馈网络（FFN）来实现

## 配置 MiniMindConfig

```python
# 在 MiniMindModel 中的关于 MoE 的配置参数
self.use_moe = use_moe
self.num_experts_per_tok = num_experts_per_tok  # 每个token选择激活的专家数量
self.n_routed_experts = n_routed_experts  		# 总的专家数量
self.n_shared_experts = n_shared_experts 		# 共享专家
self.scoring_func = scoring_func  				# 评分函数，默认为'softmax'
self.aux_loss_alpha = aux_loss_alpha  			# 负载均衡辅助损失的权重，非常关键
self.seq_aux = seq_aux  						# 是否在序列级别上计算辅助损失
self.norm_topk_prob = norm_topk_prob  			# 是否标准化top-k概率
```

## MoE门控网络（MoEGate）

这是理解整个 MoE 运作方式的钥匙，只有弄懂了这个 “门控” 或者 “调度员” 是如何工作的，后面一切才会豁然开朗

### \_\_init\_\_ 方法

该方法完成了所有的准备和设置工作，搭建了分拣中心的框架，定义了分拣的核心规则、负载均衡机制，并安装了那台最关键、可学习的智能分拣机

```python
class MoEGate(nn.Module):
    def __init__(self, config: MiniMindConfig):
        # 传统写法，调用父类的初始化函数，完成必要的初始化
        super().__init__()
        # 保存配置到示例中，方便后续访问
        self.config = config
        # 定义核心分拣规则
        self.top_k = config.num_experts_per_tok
        self.n_routed_experts = config.n_routed_experts
		# 定义评分函数，默认是 softmax
        self.scoring_func = config.scoring_func
        # 负载均衡的规则
        self.alpha = config.aux_loss_alpha
        self.seq_aux = config.seq_aux
		# 准备分拣机器和工具
        # 这一行是一个可选的附加规则，决定在选出 topk 个专家后是否将其得分归一化
        self.norm_topk_prob = config.norm_topk_prob
        # 定义 token 的向量维度，分拣机器需要知道它要读取的信息维度
        self.gating_dim = config.hidden_size
        # 这是整个分拣中心最核心的机器 —— 一个可学习的线性层
        # 通过 nn.Parameter 来告诉 PyTorch 这个矩阵是需要学习的参数，需要在训练中不断优化
        self.weight = nn.Parameter(torch.empty((self.n_routed_experts, self.gating_dim)))
        # 在启动之前，给参数一个合理的初始值，而不是一堆随机数
        self.reset_parameters()
```

为了防止分拣中心偷懒，即把 99% 的任务都交给最热门的一两个专家处理，导致其他专家空闲，整体效率低下，引入了 ”负载均衡”[^1]。

整个调度中最核心的是这个可学习的线性层，是一个形状为 `(总专家数, hidden_size)` 的权重矩阵。当一个 token 的向量（形状为 `hidden_size`）与这个矩阵相乘时，就得到一个分数向量（形状为 `总专家数`），这个分数向量代表了这个 token 与每一个专家的 “匹配度”

### reset_parameters 方法

这是一个辅助函数，用于为门控网络的权重矩阵设置一个合理的初始值

```python
def reset_parameters(self) -> None:
    import torch.nn.init as init
    init.kaiming_uniform_(self.weight, a=math.sqrt(5))
```

导入 `torch.nn.init` 模块后，调用了 `kaiming_uniform_` 函数[^2]来初始化权重矩阵

### forward 方法

这个方法是整个 MoE 架构的 ”大脑“ 和 ”决策中心“。当一批数据传来时，正是这段代码决定了他们应该被送往哪些专家，主要分为三个步骤：1、计算匹配分数；2、选出最佳专家；3、计算负载均衡损失

```python
def forward(self, hidden_states):
    # 保存原始输入形状
    bsz, seq_len, h = hidden_states.shape
	# 将输入拉平，这样做是为了一次性地为 batch 中的所有 token 计算分数
    # 拉平以后输入形状为 (bsz*seq_len, hidden_size) 也就是 (总token数，d_model)
    hidden_states = hidden_states.view(-1, h)
    # 核心计算，使用可学习的权重矩阵，与所有的 token 进行一次矩阵乘法
    # 输出的 logits 中每一个元素 logits[i, j] 都代表了第 i 个 token 与第 j 个专家的原始“匹配度”分数
    logits = F.linear(hidden_states, self.weight, None)
    if self.scoring_func == 'softmax':
        # 将原始的 logits 分数通过 softmax 函数转换为概率
        scores = logits.softmax(dim=-1)
    else:
        raise NotImplementedError(f'insupportable scoring function for MoE gating: {self.scoring_func}')

    # 选出最佳的 k 个专家，调用的 torch.topk() 函数，从上一步得到的 scores 中为每一个 token
    # 选出概率最高的 k 个专家，返回值
    # topk_weight: 这 k 个被选中专家的具体概率值，形状 (bsz*seq_len, k)
    # topk_idx: 这 k 个被选中专家的索引（ID），形状 (bsz*seq_len, k)
    topk_weight, topk_idx = torch.topk(scores, k=self.top_k, dim=-1, sorted=False)

    # 可选的权重归一化步骤，如果启用，会把 k 个专家的权重重新归一化，使其和为 1
    if self.top_k > 1 and self.norm_topk_prob:
        denominator = topk_weight.sum(dim=-1, keepdim=True) + 1e-20
        topk_weight = topk_weight / denominator

    # 如果是训练阶段，且 alpha > 0，计算负载均衡损失
    if self.training and self.alpha > 0.0:
        scores_for_aux = scores
        aux_topk = self.top_k
        topk_idx_for_aux_loss = topk_idx.view(bsz, -1)
        if self.seq_aux:
            scores_for_seq_aux = scores_for_aux.view(bsz, seq_len, -1)
            ce = torch.zeros(bsz, self.n_routed_experts, device=hidden_states.device)
            ce.scatter_add_(1, topk_idx_for_aux_loss,
                            torch.ones(bsz, seq_len * aux_topk, device=hidden_states.device)).div_(
                seq_len * aux_topk / self.n_routed_experts)
            aux_loss = (ce * scores_for_seq_aux.mean(dim=1)).sum(dim=1).mean() * self.alpha
        else:
            mask_ce = F.one_hot(topk_idx_for_aux_loss.view(-1), num_classes=self.n_routed_experts)
            ce = mask_ce.float().mean(0)
            Pi = scores_for_aux.mean(0)
            fi = ce * self.n_routed_experts
            aux_loss = (Pi * fi).sum() * self.alpha
    # 否则负载均衡损失为 0
    else:
        aux_loss = 0
    # 返回结果
    return topk_idx, topk_weight, aux_loss
```



[^1]: 指通过合理分配计算资源，确保每个专家被均衡地激活和使用，避免某些专家被过度激活，而其他专家长期闲置的情况。
[^2]: 这是一种非常著名且常用的神经网络权重初始化方法，由计算机视觉领域的大牛何恺明在其论文中提出，也被称为 “He a初始化” 。其设计初衷是为了解决深度神经网络中梯度消失 / 爆炸的问题，通过让权重满足特定的统计分布

## MoE前馈网络（MoEFeedForward）

这个模块包含了 `MoEGate` 和一个由多个标准 `FeedForward` 网络组成的专家列表 

### \_\_init\_\_ 方法

```python
class MOEFeedForward(nn.Module):
    def __init__(self, config: MiniMindConfig):
        # 传统写法，调用父类的初始化函数，完成必要的初始化
        super().__init__()
        # 保存配置到示例中，方便后续访问
        self.config = config
        # 创建专家团队
        self.experts = nn.ModuleList([
            FeedForward(config)
            for _ in range(config.n_routed_experts)
        ])
        # 创建调度员，来决定每个任务应该由哪些专家来处理
        self.gate = MoEGate(config)
        # 可选步骤，用于创建所有任务都必须咨询的通用专家
        if config.n_shared_experts > 0:
            self.shared_experts = nn.ModuleList([
                FeedForward(config)
                for _ in range(config.n_shared_experts)
            ])
```

这段代码在搭建一个混合专家的骨架，完成了以下工作：

- 创建了一组路由专家，他们是专才，等待被门控网络调用
- 创建了一个门控网络，它是调度员，负责分配任务
- 创建了一组共享专家，他们是通才，为所有任务提供基础处理

### forward 方法

这个方法描述了混合专家层在前向传播时如何实际运作的，整个过程可以看作是一次高效的 “专家会诊”

```python
def forward(self, x):
    # 保存原始输入的副本，后面计算共享专家时会用到
    identity = x
    # 记录输入的原始输入形状，因为中间计算会把数据拉平，最后需要恢复原状
    orig_shape = x.shape
    bsz, seq_len, _ = x.shape
    # 使用门控机制选择专家，返回的值分别为被选中的 k 个专家的索引、每个被选中专家的对应权重、负载均衡辅助损失
    topk_idx, topk_weight, aux_loss = self.gate(x)
    # 将输入 x 与专家的索引 topk_id_x 拉平数据以方便处理
    x = x.view(-1, x.shape[-1])
    flat_topk_idx = topk_idx.view(-1)
    # 区分训练与推理
    if self.training:
        # 如果是训练，假设每个 token 要发给两个专家，则将 token 复制两份，可以独立地被对应的专家处理
        x = x.repeat_interleave(self.config.num_experts_per_tok, dim=0)
        # 创建一个空的张量 y 来存放所有专家的计算结果
        y = torch.empty_like(x, dtype=torch.float16)
        # 遍历所有专家
        for i, expert in enumerate(self.experts):
            # 核心的分发计算
	        y[flat_topk_idx == i] = expert(x[flat_topk_idx == i]).to(y.dtype)  # 确保类型一致
        # 加权求和，将每个 token 的 k 个专家的输出乘以它们各自的权重 topk_weight，然后求和
        y = (y.view(*topk_weight.shape, -1) * topk_weight.unsqueeze(-1)).sum(dim=1)
        # 将拉平的张量 y 恢复到原始的形状 (bsz, seq_len, hidden_Size)
        y = y.view(*orig_shape)
    else:
        # 如果是推理，则调用一个专门为推理优化的 moe_infer 方法，速度更快
        y = self.moe_infer(x, flat_topk_idx, topk_weight.view(-1, 1)).view(*orig_shape)
    # 如果配置共享专家，则遍历所有共享专家，将它们的计算结果加到路由专家的输出上
    if self.config.n_shared_experts > 0:
        for expert in self.shared_experts:
       		y = y + expert(identity)
    # 保存辅助损失并返回
    self.aux_loss = aux_loss
    return y
```

核心的分发计算，

### moe_infer 方法

这段代码的核心目的是在推理模式下，以最高效的方式完成混合专家的计算。在训练模式下计算方式虽然直观，但在推理时效率低下，因为它设计大量的小规模、不连续的数据索引操作，而 `moe_infer` 方法则采取了一种完全不同的、基于 “排序-分块-散射” 的策略，来将这个过程优化到极致

```python
# 这是一个装饰器，明确告诉 PyTorch 在这个函数内不需要计算梯度。这是推理模式下的做法，可以节省大量计算和显存
@torch.no_grad()
def moe_infer(self, x, flat_expert_indices, flat_expert_weights):
    # 创建一个和输入 x 形状相同、值全为 0 的缓存张量，用来累加所有专家的计算结果
    expert_cache = torch.zeros_like(x)
    # 整个算法的灵魂
    idxs = flat_expert_indices.argsort()
    tokens_per_expert = flat_expert_indices.bincount().cpu().numpy().cumsum(0)
    token_idxs = idxs // self.config.num_experts_per_tok
    # 当tokens_per_expert = [6, 15, 20, 26]，tokens_per_expert.shape[0]即为专家数量（此时为6）
    # 且token_idxs = [3, 7, 19, 21, 24, 25,  4,  5,  6, 10, 11, 12...] 时
    # 意味token_idxs[:6] -> [3, 7, 19, 21, 24, 25]这6个位置属于专家0处理的token（每个token有可能被多个专家处理，这取决于num_experts_per_tok）
    # 接下来9个位置token_idxs[6:15] -> [4,  5,  6, 10, 11, 12...]属于专家1处理的token...依此类推
    for i, end_idx in enumerate(tokens_per_expert):
        start_idx = 0 if i == 0 else tokens_per_expert[i - 1]
        if start_idx == end_idx:
        continue
        expert = self.experts[i]
        exp_token_idx = token_idxs[start_idx:end_idx]
        expert_tokens = x[exp_token_idx]
        expert_out = expert(expert_tokens).to(expert_cache.dtype)
        expert_out.mul_(flat_expert_weights[idxs[start_idx:end_idx]])
        expert_cache.scatter_add_(0, exp_token_idx.view(-1, 1).repeat(1, x.shape[-1]), expert_out)

return expert_cache
```

